<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base brand color, used for the adaptive launcher icon background. -->
    <color name="ic_launcher_background">#1B1B2F</color>
    <color name="ic_launcher_foreground_tint">#FFE066</color>

    <!-- Pre-Compose window background, shown for a split second before
         Jetpack Compose takes over. Matches the app's default dark theme
         (see ClockSettings.isDarkTheme = true) to avoid a white flash. -->
    <color name="window_background">#101018</color>
</resources>
