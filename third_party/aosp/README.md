# Android / AOSP integration metadata

Chrona does not vendor the complete Android Open Source Project. The Android SDK, platform APIs, AndroidX, and Compose dependencies are installed/resolved by CI.

This directory stores only integration metadata and documentation for source-level compatibility decisions.
