# Copyright (c) 2026 Febrian Rahmad Cahya. All rights reserved.

name: Package Maintenance

on:
  workflow_dispatch:
  schedule:
    - cron: '17 3 * * 1'

permissions:
  contents: write

concurrency:
  group: package-maintenance
  cancel-in-progress: false

jobs:
  package:
    name: Prepare and commit package metadata
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle
      - uses: gradle/actions/setup-gradle@v6
      - uses: android-actions/setup-android@v3
      - name: Prepare SDK packages
        run: |
          yes | sdkmanager --licenses >/dev/null 2>&1 || true
          sdkmanager --install \
            'platform-tools' \
            'platforms;android-36' \
            'build-tools;36.0.0' \
            'cmake;3.31.6' \
            'ndk;28.2.13676358'
      - name: Validate package files
        run: |
          chmod +x ./gradlew
          ./gradlew --no-daemon --stacktrace tasks
          test -f gradle/wrapper/gradle-wrapper.properties
          if [ -f gradle/libs.versions.toml ]; then echo 'Version catalog present'; fi
      - name: Commit generated package metadata if changed
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add gradle .github docs/build/BUILD_ENVIRONMENT.md docs/build/sdkmanager-list.txt
          if git diff --cached --quiet; then
            echo 'No package metadata changes.'
            exit 0
          fi
          git commit -m 'chore(ci): refresh Android build packages'
          git push origin HEAD:${GITHUB_REF_NAME}
