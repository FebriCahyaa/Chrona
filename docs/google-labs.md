# Google Labs

Google Labs is treated as an external experimental ecosystem rather than as a Gradle dependency. Chrona does not add an invented or unofficial “Google Labs plugin”.

Official entry point: https://labs.google/

The project can evaluate compatible Labs experiments separately while keeping the application build deterministic.
