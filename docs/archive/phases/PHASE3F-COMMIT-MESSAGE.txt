feat(phase3): unify navigation and time-tool spatial choreography

Problem:
- Shared bounds existed on individual cards but destination surfaces were inconsistent.
- Alarm and World Clock could lose their dashboard motion identity when entering a populated destination.
- Navigation used default destination transitions, creating visual discontinuity around shared surfaces.

Changes:
- Add ChronaNavigationMotion with shared spatial enter/exit and pop transitions.
- Apply the dashboard motion key to the Alarm destination root surface.
- Apply the dashboard motion key to the Timer destination root surface.
- Apply the dashboard motion key to the World Clock destination root surface.
- Preserve the Stopwatch destination-level motion contract.
- Keep centralized timing on ChronaMotionTokens.
- Add stable motion-contract unit tests.

Behavior:
- Dashboard-to-tool navigation now uses a consistent spatial language across primary time tools.
- Shared bounds remain the dominant spatial movement while destination chrome uses low-amplitude fade/scale.
- Existing routes, ViewModels, schedulers, repositories and ChronaTimeEngine remain unchanged.

Validation:
- Static source and delimiter checks performed.
- No files deleted.
- Full Gradle build/test is not claimed in this environment.

Files:
- ChronaNavigationMotion.kt
- ChronaNavigationMotionContractTest.kt
- ChronaRootNavigation.kt
- AlarmScreen.kt
- TimerScreen.kt
- WorldClockScreen.kt
- ChronaMotionTokens.kt
- Phase 3F documentation and manifest files

Risks:
- Final visual verification still requires a Compose runtime/device.
- World Clock city-detail choreography remains a later step because the current detail destination is only represented in route constants and is not yet part of the active destination graph.
