feat(phase6): add Android 17 runtime device QA gate

Problem:
- Phase 6A validated static release integrity but there was no checked-in instrumentation smoke test or emulator execution path.
- The current preparation environment has no adb/emulator, so physical runtime verification cannot be claimed locally.

Changes:
- Add Compose instrumentation smoke tests for MainActivity startup and usability.
- Add phase6-runtime-qa.sh for static runtime preflight and optional connected-device execution.
- Add Runtime Device QA GitHub Actions workflow with an Android 17 API 37 emulator.
- Add Android 17 runtime/device QA matrix and process recreation instructions.
- Mark Phase 5 closed and Phase 6B complete at the automation gate in the roadmap.

Behavior:
- Pull requests and manual CI runs can execute connectedDebugAndroidTest on an Android 17 emulator.
- Local environments without adb report a SKIP rather than claiming device verification.
- Production app behavior is unchanged; the instrumentation suite is verification-only.

Validation:
- Phase 6A static gate passes.
- Runtime preflight source checks pass.
- Compose instrumentation configuration is present.
- Physical-device execution remains pending in a connected Android runtime.

Files:
- app/build.gradle.kts
- app/src/androidTest/java/com/febricahyaa/clockapp/ChronaRuntimeSmokeTest.kt
- scripts/phase6-runtime-qa.sh
- .github/workflows/runtime-device-qa.yml
- PHASE6B-* metadata
- ROADMAP-PHASES.md
