feat(phase6): establish static release gate and release documentation

Problem:
- Final release readiness lacked one deterministic repository-level static gate.
- README/docs referenced build and release documents that were missing from the source tree.
- Release validation was distributed across workflow steps without a reusable local gate.

Changes:
- Add scripts/phase6-release-gate.sh.
- Verify Android 17 pins, semantic version metadata, signing environment wiring,
  release workflow gates, manifest export boundaries, documentation targets,
  and secret/output hygiene.
- Run the static release gate from the manual release workflow after version derivation.
- Restore build environment and release signing/changelog documentation targets.
- Update the Phase 6 roadmap to mark 6A complete.

Behavior:
- A release workflow now fails early on static consistency or release-hygiene violations.
- Supplying a release tag to the gate requires it to match versionName after the workflow bump.
- No private signing material is required to run the static gate.

Validation:
- Phase 6A static release gate passed.
- Documentation link targets passed.
- Full Android build remains a CI/runtime gate rather than a static claim.

Files:
- Added phase6-release-gate.sh and Phase 6A handoff files.
- Added build/release documentation.
- Modified release.yml and ROADMAP-PHASES.md.

Risks:
- Physical-device behavior and signed APK verification remain Phase 6B/6C work.
