feat(phase4): refine alarm into expressive full-height editor sheet

Problem:
- The Alarm editor was functional but remained visually compact for the selected full expressive sheet concept.
- Long editor content could compete with the action row on smaller displays.
- The previous AlarmScreen artifact also contained literal \\n sequences that would break Kotlin source parsing.

Changes:
- Rebuild AlarmEditorSheet as a near full-height Material 3 ModalBottomSheet.
- Add a large selected-time preview above the existing TimePicker.
- Separate Schedule and Alert behavior into scrollable editor sections.
- Add editable enabled state alongside vibration and ringtone controls.
- Keep Cancel / Save actions pinned beneath the scrolling content.
- Add IME and navigation-bar padding.
- Preserve ALARM_EDITOR shared-motion identity.
- Preserve existing AlarmViewModel and scheduler mutation flow.
- Default legacy onEdit callback for source compatibility.
- Update Phase 4 roadmap status for 4E.

Behavior:
- Create and Edit share the same expressive editor surface.
- Cancel and swipe-dismiss discard unsaved editor state.
- Save/Create commits exactly one AlarmItem through the existing callbacks.
- Existing alarm-list interactions remain unchanged.

Validation:
- Static source audit passed.
- No files deleted.
- Full Gradle compilation remains pending due Gradle distribution availability.

Files:
- Modified AlarmScreen.kt.
- Modified ROADMAP-PHASES.md.
- Added Phase 4E documentation and metadata.

Risks:
- Final visual density and TimePicker ergonomics require physical-device verification across compact screens.
