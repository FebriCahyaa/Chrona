feat(phase4): rebuild timer as circular hero with dynamic scaling

Problem:
- Timer editor and countdown were functional but the selected Circular Hero concept was not yet reflected consistently.
- The hero used a fixed 286dp diameter, limiting responsive composition.
- Completed-state action reused the play icon even though the action was Reset.

Changes:
- Introduce BoxWithConstraints-driven circular hero sizing.
- Scale ring stroke and countdown typography from the computed hero scale.
- Use the same TIMER_EDITOR shared identity for editor and countdown surfaces.
- Preserve keypad and quick-duration editing.
- Change completed-state primary action icon to Refresh.
- Add Phase 4D documentation and validation artifacts.

Behavior:
- Timer hero adapts to available width while remaining bounded.
- Editor and countdown transitions continue using Chrona spatial motion tokens.
- Running, paused, completed, reset, preset, and persistence behavior remain unchanged at the engine/viewmodel layer.

Validation:
- Static source audit passed.
- No files deleted.
- Full Gradle compilation/test remains blocked by Gradle distribution networking.

Risks:
- Final typography, ring density, and spacing require physical-device verification.
